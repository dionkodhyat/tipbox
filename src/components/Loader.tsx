/**
 * Loader is a component used as a placeholder to indicate loading through
 * CSS animation
 * @returns {JSX.Element} - The Loader JSX element.
 */
function Loader() {
    return (
        <div className="loader">
        </div>
    )
}

export default Loader